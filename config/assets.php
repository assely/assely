<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Assets URL path
    |--------------------------------------------------------------------------
    |
    | This path is used especially for assets URLs paths generation.
    | It should points to the root of this application, same
    | as directory above, but in URI friendly format.
    |
     */

    'path' => get_template_directory_uri().'/resources/assets',

];
